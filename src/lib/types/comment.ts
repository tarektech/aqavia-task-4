export type CommentType = {
    postId: number; 
    id: number;
    name: string;
    email: string;
    body: string;
}

export type CommentFormdataType = {
    name: string;
    email: string;
    body: string;
}
